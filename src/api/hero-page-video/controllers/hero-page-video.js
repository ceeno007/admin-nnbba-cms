'use strict';

/**
 * hero-page-video controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::hero-page-video.hero-page-video');
