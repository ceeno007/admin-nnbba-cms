'use strict';

/**
 * presidents-image controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::presidents-image.presidents-image');
